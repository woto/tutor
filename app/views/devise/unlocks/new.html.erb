<% @meta_title = "Resend unlock instructions" %>
<%= meta_title class: 'text-xs-center' %>

<%= container do %>
  <%= row do %>
    <%= auth_cols do %>
      <%= page_content do %>
        <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: 'm-b-1' }) do |f| %>
          <%= f.error_notification %>
          <%= f.full_error :unlock_token %>

          <div class="form-inputs">
            <%= f.input :email, required: true, autofocus: true %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Resend unlock instructions", class: 'btn-primary' %>
          </div>
        <% end %>

        <%= render "devise/shared/links" %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
